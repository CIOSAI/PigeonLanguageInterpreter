create error queue (Array<callback>):
	pigeon.errorQueue.push(()=>something)
	at parse():
		pop errors from 0 to pigeon.errorQueue.length
mechanism to trace to the point of error:
	add "source" to PigeonNode